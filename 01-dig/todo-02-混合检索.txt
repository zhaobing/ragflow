任务 1：混合检索策略分析
代码范围: 141-157 行

核心内容:

向量检索的向量获取与阈值配置
融合表达式 FusionExpr 的权重分配（全文检索 5% + 向量检索 95%）
混合检索表达式组装逻辑（matchText + matchDense + fusionExpr）
研究要点:

为什么向量检索权重设置为 95%？这种权重分配的适用场景是什么？
similarity 参数的作用机制（0.1 阈值的意义）
Elasticsearch/Infinity 的混合检索 weighted_sum 实现原理
任务 2：空结果重试机制分析
代码范围: 159-173 行

核心内容:

检索结果为空时的降级重试策略
两种重试路径：① 去掉文档过滤条件 ② 降低匹配阈值
研究要点:

min_match=0.1 对全文检索的影响机制
similarity 从 0.1 升至 0.17 的语义（注释说是"放宽限制"需要验证）
为什么有 doc_id 过滤时的处理逻辑不同？
重试次数是否有上限？是否存在性能问题？
任务 3：关键词提取与细粒度分词分析
代码范围: 175-183 行

核心内容:

rag_tokenizer.fine_grained_tokenize() 的分词逻辑
关键词去重与长度过滤（len(kk) < 2 跳过单字）
研究要点:

fine_grained_tokenize 与常规分词的区别是什么？
为什么单字词被过滤？这对中文分词的影响
关键词扩展策略：原始关键词 + 细粒度分词的组合效果
任务 4：结果数据结构构建分析
代码范围: 185-197 行

核心内容:

SearchResult 对象的字段构成
dataStore 的数据提取方法调用链
研究要点:

query_vector 在后续流程中的用途（重打分？）
highlight 字段的高亮生成逻辑
aggregation 聚合字段的作用（文档级别统计？）
_score 字段的计算来源（ES 默认打分 vs 融合打分）
任务 5：全文检索与向量检索的协同机制
代码范围: 全段（结合上下文）

核心内容:

matchText（全文检索）的生成逻辑（需追溯到 self.qryr.question()）
get_vector() 方法的向量生成流程
两种检索方式的优势互补策略
研究要点:

全文检索依赖倒排索引，向量检索依赖 Embedding 模型
权重为 5:95 的设计是否合理？什么场景需要调整？
是否存在检索冲突（如全文检索召回但向量检索过滤掉的情况）
🎯 建议分析顺序

任务 5（整体机制） down
    ↓
任务 1（核心流程）
    ↓
任务 2（异常处理）
    ↓
任务 3（关键词优化）
    ↓
任务 4（结果封装）
我将按照你的选择，从这些维度展开深入分析。请告诉我你想从哪个任务开始，或者是否需要调整任务拆分