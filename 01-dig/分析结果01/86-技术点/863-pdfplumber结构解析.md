
### 0. 原始数据

```
{'matrix': (1, 0, 0, 1, 293.81, 731.26), 'fontname': 'BCDHEE+MicrosoftYaHei', 'adv': 12.0, 'upright': True, 'x0': 293.81, 'y0': 728.224, 'x1': 305.81, 'y1': 740.224, 'width': 12.0, 'height': 12.0, 'size': 12.0, 'mcid': 5, 'tag': 'Span', 'object_type': 'char', 'page_number': 1, 'ncs': 'DeviceGray', 'text': '大', 'stroking_color': (0,), 'stroking_pattern': None, 'non_stroking_color': (0,), 'non_stroking_pattern': None, 'top': 101.81599999999992, 'bottom': 113.81599999999992, 'doctop': 101.81599999999992}

```

```json

{
  "matrix": [1, 0, 0, 1, 293.81, 731.26],
  "fontname": "BCDHEE+MicrosoftYaHei",
  "adv": 12.0,
  "upright": true,
  "x0": 293.81,
  "y0": 728.224,
  "x1": 305.81,
  "y1": 740.224,
  "width": 12.0,
  "height": 12.0,
  "size": 12.0,
  "mcid": 5,
  "tag": "Span",
  "object_type": "char",
  "page_number": 1,
  "ncs": "DeviceGray",
  "stroking_color": [0],
  "stroking_pattern": null,
  "non_stroking_color": [0],
  "non_stroking_pattern": null,
  "top": 101.816,
  "bottom": 113.816,
  "doctop": 101.816,
  "text": "大"
}
```


这段数据是 `pdfplumber` 库在解析 PDF 时提取出的**单个字符（Character）对象**的详细属性。

`pdfplumber` 将 PDF 中的每个字符视为一个独立的对象，并提取了关于它的位置、字体、颜色和内容的详细信息。


### 1. 基础内容与标识

* **`text`**: `'大'`
* **含义**: 该对象实际对应的字符内容。


* **`object_type`**: `'char'`
* **含义**: 对象的类型。这里明确表示这是一个字符（character），而不是直线（line）、矩形（rect）或图片（image）。


* **`page_number`**: `1`
* **含义**: 该字符所在的页码（从 1 开始计数）。



### 2. 坐标与尺寸 (最关键部分)

`pdfplumber` 提供了两套坐标系统，理解它们的区别很重要。

* **`x0`**: `293.81`
* **含义**: 字符边界框（Bounding Box）的**左侧** x 坐标。


* **`x1`**: `305.81`
* **含义**: 字符边界框的**右侧** x 坐标。


* **`y0`**: `728.224`
* **含义**: 字符底部距离**页面底端**的距离（这是原生 PDF 坐标系，原点在左下角）。


* **`y1`**: `740.224`
* **含义**: 字符顶部距离**页面底端**的距离。



* **`top`**: `101.816...`
* **含义**: 字符顶部距离**当前页面顶端**的距离（`pdfplumber` 的坐标系原点在页面左上角）。


* **`bottom`**: `113.816...`
* **含义**: 字符底部距离**当前页面顶端**的距离。


* **`width`**: `12.0`
* **含义**: 字符的宽度 (`x1 - x0`)。


* **`height`**: `12.0`
* **含义**: 字符的高度。


* **`doctop`**: `101.816...`
* **含义**: 字符顶部距离**整个文档第一页顶端**的累积距离。如果这是第 2 页的字，这个值会加上第 1 页的高度。因为这里是第 1 页，所以它等于 `top`。



### 3. 字体与排版信息

* **`fontname`**: `'BCDHEE+MicrosoftYaHei'`
* **含义**: 使用的字体名称。
* *注*: `BCDHEE+` 是 PDF 的**字体子集前缀 (Subset Prefix)**，表示 PDF 中只嵌入了该字体被使用到的那部分字符，而不是整个字体文件。后面跟着的是具体的字体名“微软雅黑”。


* **`size`**: `12.0`
* **含义**: 字号大小（单位通常是 point）。


* **`adv`**: `12.0`
* **含义**: Advance width（步进宽度）。表示打印完这个字符后，光标向右移动的距离。对于等宽字体或方块字（如汉字），通常等于 `width`。


* **`upright`**: `True`
* **含义**: 字符是否是直立的。如果字符被旋转或倾斜，这里可能是 `False`。


* **`matrix`**: `(1, 0, 0, 1, 293.81, 731.26)`
* **含义**: 文本变换矩阵 (Affine Transformation Matrix)。
* 前四个数 `(1, 0, 0, 1)` 控制缩放、旋转和倾斜。这里表示无缩放、无旋转。
* 后两个数 `(293.81, 731.26)` 表示平移位置（即字符在 PDF 原始坐标系中的起始 x, y 坐标）。



### 4. 颜色与图形状态

* **`ncs`**: `'DeviceGray'`
* **含义**: Name of Color Space（颜色空间名称）。`DeviceGray` 表示灰度模式。


* **`stroking_color`**: `(0,)`
* **含义**: 描边颜色。在灰度模式下，`0` 代表黑色，`1` 代表白色。这里是黑色描边。


* **`non_stroking_color`**: `(0,)`
* **含义**: 填充颜色（也就是字体的实际颜色）。这里也是黑色。


* **`stroking_pattern`** / **`non_stroking_pattern`**: `None`
* **含义**: 更复杂的颜色填充模式（如渐变、纹理），这里没有使用。



### 5. PDF 内部结构信息 (Metadata)

* **`mcid`**: `5`
* **含义**: Marked Content ID。用于“标记 PDF (Tagged PDF)”，这有助于屏幕阅读器理解文档结构或提取特定段落的内容。


* **`tag`**: `'Span'`
* **含义**: 与 `mcid` 关联的标签类型。`Span` 通常表示这是一个行内文本片段。


---

### 总结

你提取到的这是一个位于**第1页**、字体为**12号微软雅黑**、颜色为**黑色**的汉字 **“大”**。它位于页面左侧约 293 单位，距离页面顶部约 101 单位的位置。